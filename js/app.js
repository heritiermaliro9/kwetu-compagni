(() => { const root = document.documentElement, saved = localStorage.getItem('kwetu-theme'); if (saved === 'dark' || (!saved && matchMedia('(prefers-color-scheme: dark)').matches)) root.dataset.theme = 'dark'; document.querySelectorAll('.theme-toggle').forEach(b => { const set = () => { const dark = root.dataset.theme === 'dark'; b.textContent = dark ? '☀' : '◐'; b.setAttribute('aria-label', dark ? 'Activer le mode clair' : 'Activer le mode sombre'); }; set(); b.onclick = () => { root.dataset.theme = root.dataset.theme === 'dark' ? 'light' : 'dark'; localStorage.setItem('kwetu-theme', root.dataset.theme); set(); }; }); const toggle = document.querySelector('.nav-toggle'), nav = document.querySelector('.site-nav'); if (toggle && nav) toggle.onclick = () => { const open = nav.classList.toggle('is-open'); toggle.setAttribute('aria-expanded', open); toggle.textContent = open ? '×' : '☰'; }; })();
